CONTIKI_PROJECT = RARL

TARGET = cooja

CONTIKI = /workspace/contiki-ng

CFLAGS += -DSEND_INTERVAL_MS=600  # 100 PPM
CFLAGS += -DQUEUEBUF_CONF_NUM=8

LDLIBS += -lm

include $(CONTIKI)/Makefile.include