CONTIKI = ../../../contiki-ng

# Add rpl-lite module and compile our local sources
MODULES += os/net/routing/rpl-lite
CONTIKI_PROJECT_SOURCEFILES += rpl-icpla.c

# (rest stays the same)
include $(CONTIKI)/Makefile.include
